package org.example.pages;

import org.openqa.selenium.WebDriver;

public class OrganizationInfoPage extends NavigationMenu{
    public OrganizationInfoPage(WebDriver driver) {
        super(driver);
    }
}
